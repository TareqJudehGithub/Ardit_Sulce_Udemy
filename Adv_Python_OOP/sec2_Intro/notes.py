"""
OOP

Identifiers vs objects
"""
a = 5
b = 'hello'
c = [3, 'cat']
'hello world'

def add_nums(x, y):
  return x + y

add_nums(7, 9)

"""
5, 'hello', [3, 'cat'] and 'hello world', and the func add_nums are all objects, even the items inside 
the list are object as they occupy space in memory. meanwhile, a, b, c and 
add_nums func call are considered identifiers (variables).

"""
